import React from 'react';
import { useMatches } from 'react-router-dom';

export const Breadcrumbs: React.FC = React.memo(function Breadcrumbs() {
  const matches = useMatches();

  return (
    <ol>
      
    </ol>
  );
});
